services:
  seedr:
    image: ghcr.io/rursache/seedr:latest
    container_name: seedr
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    # environment:
    #   SEEDR_USERNAME: admin
    #   SEEDR_PASSWORD: changeme
