services:
  seedr:
    build: .
    container_name: seedr
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - SEEDR_DATA_DIR=/data
      - WEB_PORT=8080
      - LOG_LEVEL=info
